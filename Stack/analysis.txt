The time complexity of clear in my Stack function is time complexity O(n) because I used the built in vector clear function. The std::vector clear() function goes through every element and deletes it induvidually, giving it time complexity O(n) with n elements